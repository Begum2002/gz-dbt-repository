name: 'gz_dbt_repository'
version: '1.0'
config-version: 2

profile: 'user'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  gz_dbt_repository:
    +materialized: view

    intermediate:
      +materialized: view

    mart:
      +materialized: table

      finance:
        +schema: finance   # BigQuery dataset adı
